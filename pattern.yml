dependencies: null
lifecycle:
  create:
    commands:
    - bash lifecycle/create.sh
  delete:
    commands:
    - bash lifecycle/delete.sh
  update:
    commands:
    - bash lifecycle/update.sh
  upgrade:
    commands:
    - bash lifecycle/upgrade.sh
metadata:
  organisation-id: onp
  pattern-categories:
  - Other
  pattern-description: Bank initiates non-card-related claims (e.g., ACH and ATM-related
    claims)
  pattern-id: onp.Initiates.non.card.related.claims.(e.g.,.ACH.and.ATM.related.claims)
  pattern-name: Initiates non-card-related claims (e.g., ACH and ATM-related claims)
  pattern-version: 1.0.0
